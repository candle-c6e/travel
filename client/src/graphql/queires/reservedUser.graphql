query ReservedUsers($paginateInput: PaginateInput!) {
  reservedUsers(paginateInput: $paginateInput) {
    reserved {
      id
      hotel_id
      name
      url
      start_date
      end_date
      days
      created_at
    }
    totalPages
  }
}
